# Optional: set your Anthropic API key to enable the full Claude-powered agent.
# Without it, the backend runs in offline keyword-matching mode.
ANTHROPIC_API_KEY=

# Server port (default 3001)
PORT=3001
